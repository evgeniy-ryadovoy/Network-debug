import Foundation

public enum Constants {
    public static let defaultRequestTimeout = TimeInterval(2)
    public static let retryCount = 5
    public static let apiVersion = "1"
    internal static let clientId = "Identifier"
    internal static let clientVer = "1"
}
